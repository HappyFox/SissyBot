syntax = "proto3";


message PingFrame {
    uint64 time = 1;
    
}

message PongFrame {
    uint64 time =1;
}

message Packet {
    oneof frame {
        PingFrame ping = 1;
        PongFrame pong = 2;
    }
}

