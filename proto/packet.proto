syntax = "proto3";


message PingFrame {
    uint32 time = 1;
}

message Packet {
    message Frames {
        oneof frame {
            PingFrame ping = 1;
        }
    }

    repeated Frames frames =2; 
}
